import { SidebarTrigger } from "../ui/sidebar";

const Navbar = () => {
  return (
    <div>
      <SidebarTrigger className="cursor-pointer" />
      <h1>YOUTUBE</h1>
    </div>
  );
};

export default Navbar;
